import '../../../styles/globals.css';
import React from 'react'

const TrafficDataLayout = ({
    children,
  }: {
    children: React.ReactNode
  }) => {
  return (
    <>{children}</>
  )
}

export default TrafficDataLayout